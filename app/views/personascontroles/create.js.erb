$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

<% if @persona.personascontroles.count == 1 %>
	$("#personascontroles").html("<%= j render 'tabla' %>");
<% else %>
	$("#personascontroles").html("<%= j render 'tabla' %>");
<% end %>

$(".clean_personascontrol").val("").trigger('change');

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

$("#personascontrol_form").collapse("hide");

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
