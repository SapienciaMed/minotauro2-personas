$("#control_form").collapse("hide");
$("#control_form").empty();

<% if @active_record.present? %>
	$("#control_<%= @active_record.id %>").replaceWith("<%= j render 'tr', control: @active_record %>");
<% end %>

$("#control_<%= @control.id %>").html("<td colspan='8'><%= j render 'controles_form' %></td>");

$("#control_<%= @control.id %>").addClass("active_control");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();

$('.datepicker').datepicker({
	dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
	monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
	dateFormat: "yy-mm-dd",
	yearRange: '1920:'+(new Date).getFullYear(),
	changeMonth: true,
	changeYear: true,
});