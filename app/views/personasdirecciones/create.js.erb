$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

<% if @persona.personasdirecciones.count == 1 %>
	$("#personasdirecciones").html("<%= j render 'tabla' %>");
<% else %>
	$("#personasdirecciones").html("<%= j render 'tabla' %>");
<% end %>

$(".clean_personasdireccion").val("").trigger('change');

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

$("#personasdireccion_form").collapse("hide");

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
